# worldasitwas-web

Hello world starter for the worldasitwas web app.

## Run locally

```sh
npm start
# http://localhost:3000
```

No dependencies. The server reads `PORT` from the environment (Railway sets it) and falls back to 3000.

## Deploy

Deployed on Railway from the `main` branch. `GET /health` returns `{"ok":true}`.
